import {  } from "../constants/JiraCloneConsts";

const initialState = {
    //
}

export const JiraCloneReducer = (state = initialState, action) => {
    switch (action.type) {

    case '':
        return { ...state };

    default:
        return { ...state };
    }
}
